import "http"
import "strings"
import "json"
import "time"

endpoint = "http://localhost:5000/posts"
req = http.request(endpoint)
response = http.get(req)

print("Response: " + response.body)

main = rule {
	true
}
